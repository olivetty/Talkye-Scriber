# Core
python-dotenv>=1.0.0
openai>=1.0.0

# Server (optional — only if running the API server)
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6
websockets>=12.0

# Local GPU backend (optional — only if DICTATE_MODE=local)
# faster-whisper>=1.0.0

# Desktop client (macOS / Linux without evdev)
pynput>=1.7.6

# VAD mode (optional — only if DICTATE_INPUT=vad)
webrtcvad>=2.0.10
openwakeword>=0.4.0

# Desktop client (Linux — installed automatically by setup.sh)
# evdev>=1.6.0
